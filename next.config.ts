/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["en", "ja"], // English and Japanese
    defaultLocale: "en",   // Default language is English
  },
};

module.exports = nextConfig;
